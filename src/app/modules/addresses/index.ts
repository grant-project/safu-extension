import reducers, { AddressesState, INITIAL_STATE } from './reducers';
import * as addressesActions from './actions';
import * as addressesTypes from './types';

export { addressesActions, addressesTypes, AddressesState, INITIAL_STATE };

export default reducers;
